import { ParameterizedCustomPipePipe } from './parameterized-custom-pipe.pipe';

describe('ParameterizedCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParameterizedCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
